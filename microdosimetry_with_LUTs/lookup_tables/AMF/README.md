# AMF Lookup Data

Place `tsed.dat` here for AMF runs. The AMF TOPAS extension expects this file
to be present in the TOPAS run directory, so the runner will stage it from this
folder into each generated run.
